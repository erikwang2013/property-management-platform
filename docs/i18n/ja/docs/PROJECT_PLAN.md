# 不動産管理システム — 全プロジェクト計画

> 生成日：2026-08-16 · 出典：pmp-team チーム監査（auditor / security-auditor / planner）

## 一、現状監査結論

**機能面：宣言と一致。** 22 業務モジュール + 12 拡張機能が全て完了、68 テーブル / 178 API、admin 58 コントローラ / 127 ルート、service 19 コントローラ / 57 ルート、Flutter Web 管理画面 42 ページ + 所有者端 13 ページ、HarmonyOS 5 ページ。docs/ の 14 ドキュメント + 35 枚の SVG は全てコードに裏付けられ、「宣言したのに未実装」の機能は見つからなかった。

**テスト：全緑（2026-08-17 現在）。** admin 193 tests / 452 assertions、service 101 tests / 385 assertions（7 skip は環境依存）、Flutter widget テスト 9 件（ログイン/ホーム/請求書ページ）。

**エンジニアリング面は既に整備済み：** 両端 docker-compose、GitHub Actions CI（PHP 構文 + 両端 phpunit + composer audit + Flutter analyze）、Dependabot、Prometheus メトリクスエンドポイント。

**技術負債（低リスク中心）：**
1. ログ蓄積：service/workerman.log 9.3M、admin/runtime/logs 5.7M（gitignore 済み、ディスク占有のみ）→ ローテーションが必要
2. README に service 端 57 モデルのドキュメント口径が未単独記載（64 は admin 端のみを指す）
3. TODO/FIXME なし、.env は未コミット、依存バージョンに異常なし — クリーン

## 二、セキュリティと品質のギャップ（security-auditor）

**18 層防御：16/18 が存在を検証済み**、実装は SECURITY_ARCHITECTURE.md と一致（認証コード、再確認、poster、SecurityFilter、AES-256-CBC、JWT、セッション制限、アカウントロック、RBAC、レート制限、hashids、フィールド暗号化、マスキング、監査ログ、CSP）。

**2 つの不一致項目（P1 は全て修正済み）：**
1. ~~security-php は service 端にのみインストール~~ → 両端の SecurityFilter に接続済み（admin + service とも 4b 深度スキャン層、SecurityGuard は遅延初期化、block 時に記録してエスカレーション）
2. ~~PDF 著作権ウォーターマーク未実装~~ → ExportController の第 18 層ウォーターマーク実装を確認済み（ExportController.php:179,206）、監査の誤報

**最優先リスク：ハードコードされたフォールバック鍵（修正済み）。** EncryptionService/暗号化設定を全て fail-fast 化（欠落または change-me なら起動時エラー）、プラグイン層の encryptable/jwt のハードコードとランダムフォールバックを削除；.env 鍵は生成された実ランダム値、CI は .env.example のコピーをロード。DB/Redis パスワードは依然プレースホルダーで、デプロイ資格情報であり、デプロイ側が注入する。

**エンジニアリングの弱点（P1 補完済み）：** CI に phpstan ジョブ（Level 5 + baseline）、両端 PHPUnit カバレッジゲート（実測ベースライン admin 1.66% / service 3.21%、ゲートはゼロ計装回帰を防止）、gitleaks 秘密鍵リークスキャン（.gitleaks.toml は環境テンプレートを許可）。

**高危険パターン確認：** eval( は Redis::eval のみ（安全）、exec 3 箇所（監視/インストールコントローラ）、md5( は token ブラックリストハッシュのみ、SQL は全て query builder — 素の文字列連結リスクなし。

## 三、戦略ポジショニング

**機能完備期 → エンジニアリング/商業化期。** 業務機能開発は完結済み（直近のコミットはドキュメント/監査/テスト補完）。次フェーズの投入方向：**CI/CD と品質ゲート、決済の本番化、監視アラートとバックアップ復旧、マルチテナント SaaS 化、モバイル端補完**。商業化パッケージは基盤あり（EDITIONS 3 バージョン + インストールウィザード + 著作権ウォーターマーク）、不足しているのは顧客に支払わせるためのエンジニアリング信頼性。

## 四、段階的ロードマップ

### P1 エンジニアリング固め（2-4 週）— システムを「信頼」させる

| 目標 | 主要タスク | 検収基準 |
|------|---------|---------|
| 品質ゲート全緑、鍵の制御可能、データを失わない | ① CI 補完：Flutter analyze、service テスト、PHPUnit カバレッジゲート、phpstan、gitleaks ② 鍵管理：.env ジェネレータ + JWT/DB 鍵ローテーションスクリプト ③ バックアップ復旧：mysqldump スクリプト + 復旧訓練マニュアル ④ ES 降格：キュー書き込み失敗のログフォールバック + 検索の MySQL LIKE 降格（延期）⑤ SQL バージョン管理：全量マイグレーションを docs/install.sql 単一エントリに統一（旧分割マイグレーション案はキャンセル、2026-08-16 統合） | CI 全緑（カバレッジ含む）；復旧訓練 30 分でデータ一致；ES 停止時も検索利用可；アップグレードスクリプト実行可 |

**P1 ステータス**：✅ 全て完了（バックアップ復旧は P8 に追記：scripts/backup.sh + docs/RECOVERY_RUNBOOK.md）。

### P2 商業化能力（4-8 週）— 顧客を「買いたい」気にさせる

| 目標 | 主要タスク | 検収基準 |
|------|---------|---------|
| 決済クローズドループ、監視可能、納品可能 | ① 決済本番化：WeChat/Alipay サンドボックス全フロー（注文→コールバック冪等→返金→照合）、資格情報を config/payment.php + env に集中 ② 監視アラート：Prometheus+Grafana オーケストレーション + アラートルール（5xx、ES/Redis 接続、キュー滞留）+ ログローテーション ③ 商業版制御：EDITIONS ベースのバージョンスイッチ（Lite/Standard/Full ルートグループ活性化）+ Demo データ ④ インストールウィザードで決済/ES 設定をカバー | サンドボックス決済全フロー合格（重複コールバック冪等含む）；アラート実測発動；3 バージョンスイッチをデモ可能；新環境 10 分でインストール完了 |

**P2 現状**：✅ オフライン部分は全て完了（2026-08-16/17）：決済全チェーンコード（PaymentService 注文/コールバック冪等/返金/照合 + config/payment.php 資格情報集中化 + PaymentServiceTest 純関数カバレッジ）、監視オーケストレーション（デュアル Prometheus スタック + 6 アラートルール + 両端 Grafana dashboard provisioning）、バージョンスイッチ（EDITIONS 3 バージョン + fail-fast 検証）、インストールウィザード（決済設定自動有効化含む + テンプレートパスバグ修正済み）。残る外部依存：**サンドボックス連携は資格情報待ち**（WECHAT_PAY_* / ALIPAY_* サンドボックス資格情報取得後 `scripts/payment_sandbox_smoke.php` 実行）、**アラート実測はデプロイ待ち**（`scripts/verify_monitoring.sh` でローカル検証可、実トリガー検証はデプロイ後実行）。

### P3 スケーリング（8-12 週）— システムを「多く売れる」ように

| 目標 | 主要タスク | 検収基準 |
|------|---------|---------|
| マルチテナント、性能達成、モバイル端補完 | ① マルチテナント SaaS：グループ管理を起点に、management_community に tenant_id 追加 + ミドルウェア分離（方案評審を先行、独立 DB は進化方向）② 性能負荷試験：wrk/k6 でログイン/料金/ダッシュボードを負荷、スロークエリ + Redis キャッシュ再調査 ③ モバイル端補完：HarmonyOS 5 ページをコアパスに拡張（支払い/修理依頼/お知らせ/来訪者/駐車）、Flutter 所有者端のモバイル対応 ④ オープン API / Webhook（任意） | テナント越権テスト合格；コア API P95 < 300ms；HarmonyOS コアパス完了 |

**P3 ステータス**：✅ 全て完了（2026-08-16 納品：マルチテナント三種の神器 + 負荷試験実測 P95 達成 + HarmonyOS を 5 ページに拡張）。

### P4-P9 追加納品記録（2026-08-16 ~ 08-17、当初 P1-P3 の範囲を超える新規エンジニアリング段階）

| 段階 | 納品内容 | ステータス |
|------|---------|------|
| P4 仕上げ | ES 書き込みパスフォールバック（AdminUser Searchable try/catch + ログ降格）、インストールウィザード決済設定（資格情報入力済みなら自動有効化）、ログローテーション（logrotate.conf）、監視アラートオーケストレーション（デュアル Prometheus スタック + 6 ルール） | ✅ 完了 |
| P5 残項目 | Webhook 配信（HMAC-SHA256 署名 + 指数バックオフ再試行 + 3 トリガーポイント）、料金/承認/SLA 業務単体テスト | ✅ 完了 |
| P6 デプロイ仕上げ | ワンクリックデプロイスクリプト（deploy.sh：pull → .env → compose → install.sql 冪等インポート → 監視スモーク）、コンテナログマウント修正、CI カバレッジゲート引き上げ、スケールアップ計画（SCALING_PLAN） | ✅ 完了 |
| P7 テスト深化 | service 単体テスト 43→82、Flutter widget テスト 3 ページ 8 ケース（CI 統合）、オープン API（/open 3 読み取り専用エンドポイント + X-API-Key 認証 + gen_api_key.php）、負荷試験スモーク（k6 smoke.js + workflow_dispatch 手動 workflow） | ✅ 完了 |
| P8 運用仕上げ | バックアップ復旧の定着（backup.sh + RECOVERY_RUNBOOK）、Grafana dashboard（service 端 7 パネル provisioning）、admin 単体テスト +11（152 全緑）、**インストールウィザードテンプレートパスバグ修正**（テンプレートを app/view/install へ移動、curl 実測レンダリング 200） | ✅ 完了 |
| P9 エンジニアリング仕上げ | 失実バックアップスクリプト削除（git rm 両端 4 ファイル + 8 箇所のドキュメント統一）、admin 端 Grafana dashboard（open_admin_* 接頭辞 7 パネル）、InstallValidator 検証純関数抽出 + 17 ケース（admin 193 全緑） | ✅ 完了 |

**P4-P9 集計**：admin 単体テスト 93→193、service 単体テスト 43→101、Flutter widget テスト 9/9、オープン API 3 エンドポイント、両端監視パネル対称、バックアップ/復旧/鍵/デプロイの運用スクリプト一式準備完了。

## 五、Top 10 優先アクション項目（投入産出比順）

| # | アクション項目 | 影響 | コスト | リスク | ステータス |
|---|--------|------|------|------|------|
| 1 | 鍵管理：env ジェネレータ + ローテーションスクリプト + ハードコードフォールバック鍵の削除（起動時 change-me 検証） | 高（セキュリティコンプライアンス） | 低 | 低 | ✅ 完了 |
| 2 | CI 補完：Flutter analyze + service テスト + カバレッジゲート + phpstan + gitleaks | 高（品質の下限） | 低 | 低 | ✅ 完了（P7 で flutter test 追加） |
| 3 | バックアップスクリプト + 復旧訓練 | 高（データを失わない） | 低 | 低 | ✅ 完了（P8：backup.sh + RECOVERY_RUNBOOK） |
| 4 | ES 降格フォールバック（MySQL LIKE） | 高（可用性） | 低 | 中（デュアルクエリパス保守） | ✅ 完了（P4：書き込みパス try/catch フォールバック；検索は元々全て MySQL LIKE） |
| 5 | 決済サンドボックス全フロー + コールバック冪等検証 | 高（商業化必須） | 中 | 中（資格情報/コールバックセキュリティ） | 🔶 コード完了、サンドボックス連携は資格情報待ち |
| 6 | Prometheus + Grafana アラート + ログローテーション | 高（運用性） | 中 | 低 | ✅ 完了（両端パネル P8/P9 補完；アラート実測はデプロイ待ち） |
| 7 | SQL 移行管理（全量を install.sql 単一エントリに統合） | 中（アップグレード性） | 低 | 低 | ✅ 完了（2026-08-16 統合） |
| 8 | マルチテナント方案評審 + tenant_id 分離 | 高（天井） | 高 | 高（全クエリに影響） | ✅ 完了（P3 納品、越権テスト合格） |
| 9 | 負荷試験 + スロークエリ対策 | 中（性能） | 中 | 低 | ✅ 完了（P3 実測 P95 達成；P7 スモーク版を CI に） |
| 10 | 商業版ライセンススイッチ + Demo データ | 中（プリセールス） | 中 | 低 | ✅ 完了（EDITIONS + demo_data.php + デモフロー文書） |

## 六、リスクと依存

| リスク | 現状 | 緩和提案 | ステータス |
|------|------|---------|------|
| 単機デプロイで HA なし | docker-compose 単機、MySQL/Redis/ES 同機 | バックアップ復旧訓練 + 監視アラート + スケールアップ計画文書 | ✅ 緩和定着（P8 バックアップ + P2/P8/P9 監視 + P6 SCALING_PLAN）；訓練実行はデプロイ待ち |
| ES ハード依存 | 検索/インデックス同期が全て ES 経由 | 降格フォールバック + インデックス再構築スクリプト | ✅ 緩和（P4 書き込みパスフォールバック；検索は元々全て MySQL LIKE、ES はクエリパス依存ではない） |
| 鍵管理 | プレースホルダー + ハードコードフォールバック鍵、ローテーションなし | ジェネレータ + ローテーションスクリプト；本番は環境変数注入 | ✅ 解決済み（fail-fast 検証 + gen_env_keys.sh + rotate_keys.sh） |
| 決済資格情報の散在 | 決済モジュールに集中設定なし、サンドボックス未検証 | 設定集中化 + サンドボックス先行 | 🔶 設定は集中化済み（config/payment.php）、サンドボックス連携は資格情報待ち |
| 移行管理 | 単一ファイル install.sql（IF NOT EXISTS 冪等） | 単一全量エントリに統一済み；増分アップグレードパスが必要なら別途協議 | ✅ 統一済み（2026-08-16 統合） |
| テストカバレッジが構造偏重 | 133 テストが schema/セキュリティ/往復に集中 | カバレッジゲート + コア業務（料金/承認/SLA）の単体テスト補完 | ✅ 強化済み（admin 193 / service 101 / Flutter 9；ゲートでゼロ計装回帰防止） |
| モバイル端ギャップ | HarmonyOS は 5 ページのみ、所有者端にネイティブ App なし | P3 コアパス補完；依存：HarmonyOS テストデバイス | ✅ コアパス補完（5 ページ：支払い/修理依頼/お知らせ/来訪者/駐車）；実機検証はデバイス待ち |

## 七、チーム分担（pmp-team）

| ロール | 投入タスク |
|------|---------|
| アーキテクチャ | マルチテナント方案評審と tenant_id 分離設計（P3）、ES 降格アーキテクチャ、監視アーキテクチャ（P2）、決済コールバック冪等設計評審 |
| バックエンド | 鍵生成/ローテーションスクリプト、ES 降格実装、決済設定集中化 + サンドボックス連携、移行スクリプト分割、負荷試験とスロークエリ対策 |
| Flutter フロント | CI に flutter analyze 統合、所有者端モバイル対応（P3）、バージョンスイッチ UI 連携 |
| HarmonyOS | コアパス補完：支払い/修理依頼/お知らせ/来訪者/駐車（P3） |
| テスト | カバレッジゲート、決済サンドボックスケース（重複コールバック/返金/照合）、負荷試験スクリプト、バックアップ復旧訓練実行 |
| レビュー | 決済とセキュリティの重要パス review、gitleaks の CI 組み込み、マルチテナント越権テストレビュー |
| ドキュメント | デプロイ/運用マニュアル（復旧訓練含む）、マルチテナント方案文書、監視設定マニュアル、商業版納品マニュアル |

## 八、即時アクション提案

✅ 当初 P1 の第 1-4 項目（鍵強化 → CI 補完 → バックアップスクリプト → ES 降格）は全て実行済み（P4-P9 の反復納品）。

**残る依存項目（外部条件が必要、コードギャップではない）**：
1. 決済サンドボックス連携：WECHAT_PAY_* / ALIPAY_* サンドボックス資格情報取得後 `scripts/payment_sandbox_smoke.php` を実行（注文→重複コールバック冪等→返金→照合の全チェーン検証）
2. 監視アラート実測：デプロイ後 `scripts/verify_monitoring.sh` を実行してルールロードを検証し、実際に 5xx/接続障害をトリガーしてアラートを検証（Http5xxRatio ルールは直接有効化可能）
3. バックアップ復旧訓練：docs/RECOVERY_RUNBOOK.md に従い四半期訓練を実施し、実測所要時間を記録（RTO ≤ 1h 目標）
4. HarmonyOS 実機検証：テストデバイス到着後コアパス（支払い/修理依頼/お知らせ/来訪者/駐車）を実行
