# نظام إدارة العقارات — خطة المشروع الشاملة

> تاريخ الإنشاء: 2026-08-16 · المصدر: تدقيق فريق pmp-team (auditor / security-auditor / planner)

## أولاً: خلاصة تدقيق الوضع الحالي

**الوظائف: متوافقة مع المعلن.** اكتملت 22 وحدة أعمال + 12 وظيفة موسعة، 68 جدولاً / 178 API، admin 58 متحكمًا / 127 مسارًا، service 19 متحكمًا / 57 مسارًا، لوحة إدارة Flutter Web 42 صفحة + بوابة الملاك 13 صفحة، HarmonyOS 5 صفحات. وثائق docs/ الـ14 + 35 SVG مدعومة بالكود، ولم يُعثر على أي وظيفة "معلنة غير منفذة".

**الاختبارات: خضراء بالكامل (حتى 2026-08-17).** admin 193 اختبارًا / 452 تأكيدًا، service 101 اختبار / 385 تأكيدًا (7 skips لاعتمادها على البيئة)، اختبارات Flutter widget 9 حالات (تسجيل الدخول/الرئيسية/صفحة الفواتير).

**الجاهزية الهندسية:** docker-compose للطرفين، GitHub Actions CI (فحص PHP + phpunit للطرفين + composer audit + Flutter analyze)، Dependabot، نقطة مقاييس Prometheus.

**الدين التقني (منخفض الخطورة غالبًا):**
1. تراكم السجلات: service/workerman.log 9.3M، admin/runtime/logs 5.7M (موجود في gitignore، يستهلك مساحة فقط) ← يحتاج تدويرًا
2. README لا يفرد توثيق 57 نموذجًا لطرف service (الـ64 تشير إلى طرف admin فقط)
3. لا TODO/FIXME، .env غير مضمّن في المستودع، لا شذوذ في إصدارات التبعيات — نظيف

## ثانياً: فجوات الأمان والجودة (security-auditor)

**دفاع من 18 طبقة: 16/18 تم التحقق من وجودها**، والتنفيذ متوافق مع SECURITY_ARCHITECTURE.md (رمز التحقق، التأكيد الثانوي، poster، SecurityFilter، AES-256-CBC، JWT، تقييد الجلسات، قفل الحساب، RBAC، تحديد المعدل، hashids، تشفير الحقول، إخفاء البيانات، سجلات التدقيق، CSP).

**بندان غير متوافقين (كلاهما P1 تم إصلاحه):**
1. ~~security-php مثبت على طرف service فقط~~ → تم ربط SecurityFilter بالطرفين (admin + service كلاهما بطبقة فحص عمق 4b، SecurityGuard تهيئة كسولة، يسجل ويصعّد عند الحظر)
2. ~~علامة مائية لحقوق النشر في PDF غير منفذة~~ → تم التحقق من أن الطبقة 18 للعلامة المائية في ExportController منفذة (ExportController.php:179,206)، بلاغ تدقيق خاطئ

**الأعلى خطورة: مفتاح احتياطي مكتوب يدويًا (تم إصلاحه).** EncryptionService/إعدادات التشفير كلها fail-fast (نقص أو change-me يؤدي لخطأ عند الإقلاع)، وأُزيلت الكتابة اليدوية والتراجع العشوائي في طبقة الإضافات encryptable/jwt؛ مفاتيح .env قيم عشوائية حقيقية مولدة، وCI يحمّل من نسخة .env.example. كلمات مرور DB/Redis ما زالت placeholders، وهي بيانات نشر تُحقن من جهة النشر.

**قصور هندسي (تم سده P1):** CI أضاف وظيفة phpstan (Level 5 + baseline)، وبوابات تغطية PHPUnit للطرفين (قياس فعلي للخط الأساسي admin 1.66% / service 3.21%، البوابة تمنع انحدار الصفر)، ومسح تسرب المفاتيح gitleaks (.gitleaks.toml يمرر قوالب البيئة).

**فحص الأنماط عالية الخطورة:** eval( فقط Redis::eval (آمن)، exec 3 مواضع (متحكمات المراقبة/التثبيت)، md5( فقط تجزئة القائمة السوداء للتوكنات، SQL كلها عبر query builder — لا خطر ربط خام.

## ثالثاً: التموضع الاستراتيجي

**مرحلة اكتمال الوظائف ← مرحلة الهندسة/التسويق.** اكتمل تطوير وظائف الأعمال (الالتزامات الأخيرة كلها توثيق/تدقيق/تكملة اختبارات). اتجاه الاستثمار في المرحلة التالية: **CI/CD وبوابات الجودة، إنتاجية الدفع، مراقبة الإنذار والنسخ الاحتياطي والاستعادة، تحويل SaaS متعدد المستأجرين، استكمال الجوال**. تغليف التسويق له أساس (إصدارات EDITIONS الثلاثة + معالج التثبيت + علامة حقوق النشر)، والمفقود هو مصداقية هندسية تجعل العملاء يدفعون.

## رابعاً: خارطة طريق مرحلية

### P1 ترسيخ الهندسة (2-4 أسابيع) — جعل النظام "موثوقًا"

| الهدف | المهام الرئيسية | معايير القبول |
|------|---------|---------|
| بوابات الجودة خضراء بالكامل، المفاتيح قابلة للتحكم، لا فقدان للبيانات | ① إكمال CI: Flutter analyze، اختبارات service، بوابة تغطية PHPUnit، phpstan، gitleaks ② إدارة المفاتيح: مولّد .env + سكربتات تدوير JWT/DB ③ النسخ الاحتياطي والاستعادة: سكربت mysqldump + دليل تمرين الاستعادة ④ تخفيض ES: كتابة قائمة انتظار فاشلة تنخفض لسجل احتياطي + بحث ينخفض لـ MySQL LIKE (مؤجل) ⑤ إدارة إصدارات SQL: توحيد الترحيلات الكاملة في docs/install.sql كمدخل واحد (أُلغي خطة الترحيلات المقسمة، دُمجت 2026-08-16) | CI أخضر بالكامل شاملاً التغطية؛ تمرين الاستعادة 30 دقيقة باتساق البيانات؛ البحث متاح عند توقف ES؛ سكربت الترقية قابل للتنفيذ |

**حالة P1**: ✅ اكتمل بالكامل (النسخ الاحتياطي والاستعادة موثقان في P8: scripts/backup.sh + docs/RECOVERY_RUNBOOK.md).

### P2 القدرات التسويقية (4-8 أسابيع) — جعل العملاء "يجرؤون على الشراء"

| الهدف | المهام الرئيسية | معايير القبول |
|------|---------|---------|
| حلقة دفع مغلقة، قابلة للمراقبة والتسليم | ① إنتاجية الدفع: سير كامل في صندوق رمل WeChat/Alipay (إصدار طلب ← استدعاء idempotent ← استرداد ← تسوية)، تركيز الاعتمادات في config/payment.php + env ② مراقبة الإنذار: تنسيق Prometheus+Grafana + قواعد إنذار (5xx، اتصالات ES/Redis، تراكم قائمة الانتظار) + تدوير السجلات ③ التحكم بالإصدار التجاري: مفاتيح إصدارات مبنية على EDITIONS (تفعيل مجموعات مسارات Lite/Standard/Full) + بيانات Demo ④ يغطي معالج التثبيت إعدادات الدفع/ES | سير صندوق الرمل الكامل ناجح (شاملاً تكرار الاستدعاء idempotent)؛ الإنذارات تُختبر فعليًا؛ مفاتيح الإصدارات الثلاثة قابلة للعرض؛ بيئة جديدة تُثبّت خلال 10 دقائق |

**حالة P2 الحالية**: ✅ الجزء غير المتصل اكتمل بالكامل (2026-08-16/17): كود سلسلة الدفع الكاملة (PaymentService إصدار طلب/استدعاء idempotent/استرداد/تسوية + تركيز الاعتمادات config/payment.php + تغطية PaymentServiceTest للدوال النقية)، تنسيق المراقبة (مكدسا Prometheus + 6 قواعد إنذار + provisioning لوحات Grafana للطرفين)، مفاتيح الإصدارات (إصدارات EDITIONS الثلاثة + تحقق fail-fast)، معالج التثبيت (شاملاً التفعيل التلقائي لإعداد الدفع + إصلاح خطأ مسار القالب). الاعتماد الخارجي المتبقي: **ربط صندوق الرمل بانتظار الاعتمادات** (بعد الحصول على اعتمادات WECHAT_PAY_* / ALIPAY_* شغّل `scripts/payment_sandbox_smoke.php`)، **اختبار الإنذارات الفعلي بانتظار النشر** (`scripts/verify_monitoring.sh` يتحقق محليًا، والتحقق الفعلي من الإطلاق بعد النشر).

### P3 التوسع (8-12 أسبوعًا) — جعل النظام "قابلًا للبيع بأكثر"

| الهدف | المهام الرئيسية | معايير القبول |
|------|---------|---------|
| متعدد المستأجرين، أداء مطابق، استكمال الجوال | ① SaaS متعدد المستأجرين: يبدأ بإدارة المجموعة، management_community يضاف tenant_id + عزل بالوسيطة (مراجعة الحل أولًا، قاعدة مستقلة اتجاه تطور) ② اختبار الأداء: wrk/k6 يختبر تسجيل الدخول/الرسوم/لوحة القيادة، مراجعة الاستعلامات البطيئة + ذاكرة Redis ③ استكمال الجوال: توسيع صفحات HarmonyOS الخمس إلى المسارات الأساسية (الدفع/الإصلاح/الإعلان/الزوار/المواقف)، تكييف Flutter بوابة الملاك للجوال ④ API مفتوح / Webhook (اختياري) | اختبار تجاوز صلاحيات المستأجر ناجح؛ P95 للواجهات الأساسية < 300ms؛ اكتمال المسارات الأساسية لـ HarmonyOS |

**حالة P3**: ✅ اكتمل بالكامل (تسليم 2026-08-16: ثلاثية متعدد المستأجرين + اختبار تحميل فعلي P95 محقق + توسيع HarmonyOS إلى 5 صفحات).

### سجلات تسليم إضافية P4-P9 (2026-08-16 ~ 08-17، مراحل هندسية جديدة خارج نطاق P1-P3 الأصلي)

| المرحلة | محتوى التسليم | الحالة |
|------|---------|------|
| P4 الاختتام | احتياط مسار كتابة ES (try/catch في AdminUser Searchable + تخفيض السجلات)، إعداد دفع معالج التثبيت (تفعيل تلقائي عند توفر الاعتمادات)، تدوير السجلات (logrotate.conf)، تنسيق مراقبة الإنذار (مكدسا Prometheus + 6 قواعد) | ✅ اكتمل |
| P5 بقية البنود | تسليم Webhook (توقيع HMAC-SHA256 + إعادة محاولة بالتراجع الأسي + 3 نقاط تشغيل)، اختبارات وحدة الرسوم/الموافقات/SLA | ✅ اكتمل |
| P6 اختتام النشر | سكربت نشر بخطوة واحدة (deploy.sh: pull ← .env ← compose ← استيراد install.sql idempotent ← فحص مبدئي للمراقبة)، إصلاح تركيب سجلات الحاويات، رفع بوابة تغطية CI، خطة التوسع (SCALING_PLAN) | ✅ اكتمل |
| P7 عمق الاختبارات | service unit tests 43←82، اختبارات Flutter widget 3 صفحات 8 حالات (مدمجة في CI)، API مفتوح (/open 3 نقاط قراءة فقط + مصادقة X-API-Key + gen_api_key.php)، فحص تحميل مبدئي (k6 smoke.js + workflow_dispatch يدوي) | ✅ اكتمل |
| P8 اختتام التشغيل | تنفيذ النسخ الاحتياطي والاستعادة (backup.sh + RECOVERY_RUNBOOK)، لوحة Grafana (7 لوحات provisioning لطرف service)، admin unit tests +11 (152 أخضر بالكامل)، **إصلاح خطأ مسار قالب معالج التثبيت** (نقل القالب إلى app/view/install، curl اختبر العرض 200) | ✅ اكتمل |
| P9 اختتام الهندسة | تنظيف سكربتات النسخ الاحتياطي غير الصحيحة (git rm 4 ملفات بالطرفين + توحيد 8 مواضع توثيقية)، لوحة Grafana لطرف admin (7 لوحات بادئة open_admin_*)، استخراج دوال التحقق النقية InstallValidator + 17 حالة (admin 193 أخضر بالكامل) | ✅ اكتمل |

**ملخص P4-P9**: admin unit tests 93←193، service unit tests 43←101، اختبارات Flutter widget 9/9، API مفتوح 3 نقاط، لوحات مراقبة متماثلة بالطرفين، مجموعة سكربتات تشغيل كاملة للنسخ الاحتياطي/الاستعادة/المفاتيح/النشر.

## خامسًا: أهم 10 بنود عمل ذات الأولوية (حسب نسبة العائد للاستثمار)

| # | بند العمل | الأثر | التكلفة | الخطورة | الحالة |
|---|--------|------|------|------|------|
| 1 | إدارة المفاتيح: مولّد env + سكربت تدوير + إزالة المفاتيح الاحتياطية المكتوبة يدويًا (تحقق عند الإقلاع من أنها غير change-me) | عالٍ (امتثال أمني) | منخفض | منخفض | ✅ اكتمل |
| 2 | إكمال CI: Flutter analyze + اختبارات service + بوابة التغطية + phpstan + gitleaks | عالٍ (خط الجودة الأدنى) | منخفض | منخفض | ✅ اكتمل (P7 أضاف flutter test) |
| 3 | سكربت النسخ الاحتياطي + تمرين الاستعادة | عالٍ (عدم فقدان البيانات) | منخفض | منخفض | ✅ اكتمل (P8: backup.sh + RECOVERY_RUNBOOK) |
| 4 | احتياط تخفيض ES (MySQL LIKE) | عالٍ (التوفر) | منخفض | متوسط (صيانة مساري استعلام) | ✅ اكتمل (P4: try/catch لمسار الكتابة؛ البحث أصلاً كله MySQL LIKE) |
| 5 | سير صندوق الرمل الكامل للدفع + التحقق من idempotent للاستدعاء | عالٍ (ضروري للتسويق) | متوسط | متوسط (أمان الاعتمادات/الاستدعاء) | 🔶 الكود اكتمل، ربط صندوق الرمل بانتظار الاعتمادات |
| 6 | Prometheus + Grafana إنذار + تدوير السجلات | عالٍ (قابلية التشغيل) | متوسط | منخفض | ✅ اكتمل (لوحات الطرفين P8/P9؛ اختبار الإنذار الفعلي بانتظار النشر) |
| 7 | إدارة ترحيلات SQL (دمج الكامل في install.sql كمدخل واحد) | متوسط (قابلية الترقية) | منخفض | منخفض | ✅ اكتمل (دُمج 2026-08-16) |
| 8 | مراجعة حل متعدد المستأجرين + عزل tenant_id | عالٍ (السقف) | عالٍ | عالٍ (يؤثر على كل الاستعلامات) | ✅ اكتمل (تسليم P3، اختبار تجاوز الصلاحيات ناجح) |
| 9 | اختبار التحميل + معالجة الاستعلامات البطيئة | متوسط (الأداء) | متوسط | منخفض | ✅ اكتمل (P3 تشغيل فعلي P95 محقق؛ P7 نسخة فحص مبدئي في CI) |
| 10 | مفتاح ترخيص الإصدار التجاري + بيانات Demo | متوسط (ما قبل البيع) | متوسط | منخفض | ✅ اكتمل (EDITIONS + demo_data.php + وثيقة سير العرض) |

## سادسًا: المخاطر والاعتماديات

| الخطر | الوضع الحالي | اقتراح التخفيف | الحالة |
|------|------|---------|------|
| نشر أحادي بلا HA | docker-compose أحادي، MySQL/Redis/ES على نفس الجهاز | تمرين النسخ الاحتياطي والاستعادة + مراقبة الإنذار + وثيقة خطة التوسع | ✅ التخفيف نُفذ (P8 نسخ احتياطي + P2/P8/P9 مراقبة + P6 SCALING_PLAN)؛ تنفيذ التمرين بانتظار النشر |
| اعتماد صلب على ES | البحث/مزامنة الفهرس كلها عبر ES | احتياط التخفيض + سكربت إعادة بناء الفهرس | ✅ تم التخفيف (P4 احتياط مسار الكتابة؛ البحث أصلاً كله MySQL LIKE، ES ليس اعتمادًا في مسار الاستعلام) |
| إدارة المفاتيح | placeholders + مفاتيح احتياطية مكتوبة يدويًا بلا تدوير | مولّد + سكربت تدوير؛ الإنتاج يحقن متغيرات البيئة | ✅ حُل (تحقق fail-fast + gen_env_keys.sh + rotate_keys.sh) |
| تشتت اعتمادات الدفع | وحدة الدفع بلا إعداد مركزي، صندوق الرمل غير موثق | تركيز الإعداد + صندوق الرمل أولًا | 🔶 الإعداد رُكّز (config/payment.php)، ربط صندوق الرمل بانتظار الاعتمادات |
| إدارة الترحيلات | ملف install.sql واحد (IF NOT EXISTS idempotent) | وُحّد في مدخل كامل واحد؛ مسار ترقية تدريجي عند الحاجة يُناقش لاحقًا | ✅ وُحّد (دُمج 2026-08-16) |
| تغطية الاختبارات بنيوية | 133 اختبارًا مركّزة على schema/الأمان/الذهاب والإياب | بوابة التغطية + تكملة unit tests للوظائف الأساسية (الرسوم/الموافقات/SLA) | ✅ تم التعزيز (admin 193 / service 101 / Flutter 9؛ البوابة تمنع انحدار الصفر) |
| فجوة الجوال | HarmonyOS 5 صفحات فقط، بوابة الملاك بلا تطبيق أصلي | استكمال المسارات الأساسية P3؛ الاعتماد: أجهزة اختبار HarmonyOS | ✅ اكتملت المسارات الأساسية (5 صفحات: الدفع/الإصلاح/الإعلان/الزوار/المواقف)؛ تحقق الجهاز الحقيقي بانتظار الجهاز |

## سابعًا: تقسيم الفريق (pmp-team)

| الدور | المهام المشغولة |
|------|---------|
| الهندسة المعمارية | مراجعة حل متعدد المستأجرين وتصميم عزل tenant_id (P3)، معمارية تخفيض ES، معمارية المراقبة (P2)، مراجعة تصميم idempotent لاستدعاء الدفع |
| الخلفية | سكربتات توليد/تدوير المفاتيح، تنفيذ تخفيض ES، تركيز إعداد الدفع + ربط صندوق الرمل، تقسيم سكربتات الترحيل، اختبار التحميل ومعالجة الاستعلامات البطيئة |
| واجهة Flutter | دمج flutter analyze في CI، تكييف بوابة الملاك للجوال (P3)، التعاون في واجهة مفاتيح الإصدارات |
| HarmonyOS | استكمال المسارات الأساسية: الدفع/الإصلاح/الإعلان/الزوار/المواقف (P3) |
| الاختبار | بوابة التغطية، حالات صندوق رمل الدفع (استدعاء متكرر/استرداد/تسوية)، سكربتات اختبار التحميل، تنفيذ تمرين النسخ الاحتياطي والاستعادة |
| المراجعة | مراجعة مسارات الدفع والأمان الحرجة، gitleaks في CI، مراجعة اختبارات تجاوز صلاحيات متعدد المستأجرين |
| التوثيق | دليل النشر/التشغيل (شاملاً تمرين الاستعادة)، وثيقة حل متعدد المستأجرين، دليل إعداد المراقبة، دليل تسليم الإصدار التجاري |

## ثامنًا: اقتراحات الإجراء الفوري

✅ البنود 1-4 من P1 الأصلي (تقوية المفاتيح ← إكمال CI ← سكربت النسخ الاحتياطي ← تخفيض ES) نُفذت جميعها (تسليمات متكررة P4-P9).

**الاعتماديات المتبقية (تتطلب شروطًا خارجية، ليست فجوات كود)**:
1. ربط صندوق رمل الدفع: بعد الحصول على اعتمادات WECHAT_PAY_* / ALIPAY_* شغّل `scripts/payment_sandbox_smoke.php` (تحقق كامل السلسلة: إصدار طلب ← استدعاء متكرر idempotent ← استرداد ← تسوية)
2. اختبار إنذارات المراقبة الفعلي: بعد النشر شغّل `scripts/verify_monitoring.sh` للتحقق من تحميل القواعد، وشغّل فعليًا 5xx/فشل اتصال للتحقق من الإنذار (قاعدة Http5xxRatio قابلة للتفعيل المباشر)
3. تمرين النسخ الاحتياطي والاستعادة: نفّذ تمرينًا ربع سنويًا وفق docs/RECOVERY_RUNBOOK.md وسجّل زمن القياس الفعلي (هدف RTO ≤ 1h)
4. التحقق على جهاز HarmonyOS حقيقي: بعد توفر جهاز الاختبار شغّل المسارات الأساسية (الدفع/الإصلاح/الإعلان/الزوار/المواقف)
